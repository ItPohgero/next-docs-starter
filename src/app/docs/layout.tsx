import React, { PropsWithChildren } from 'react'
import DocsLayout from 'src/layouts/docs'

const Layout = ({ children }: PropsWithChildren) => <DocsLayout>{children}</DocsLayout>
export default Layout
